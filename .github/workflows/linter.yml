---
on:
  - pull_request
  - push

name: linter

jobs:
  linter:
    uses: php-forge/actions/.github/workflows/super-linter.yml@v2
    permissions:
      checks: write
      contents: read
      statuses: write
    secrets:
      AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
